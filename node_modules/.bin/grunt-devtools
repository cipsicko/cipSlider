#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../grunt-devtools/bin/grunt-devtools" "$@"
  ret=$?
else 
  node  "$basedir/../grunt-devtools/bin/grunt-devtools" "$@"
  ret=$?
fi
exit $ret
